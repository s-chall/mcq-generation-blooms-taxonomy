[project]
name = "blooms-taxonomy-analysis"
version = "0.1.0"
description = "Reproducible validation and summary analysis for Bloom-aligned MCQ data"
readme = "README.md"
requires-python = ">=3.11"

[tool.uv]
package = false
